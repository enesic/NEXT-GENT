# Production Configuration
# Backend deployed to Vercel - WebSocket service optional (can be added later)

# Vercel Backend REST API ✅ DEPLOYED
VITE_API_BASE_URL=https://backend-one-beta-75.vercel.app/api/v1

# Railway WebSocket Service (OPTIONAL - leave empty, system works without it)
# Add later for real-time features: wss://your-websocket-service.railway.app
VITE_WS_URL=

# Note: System is fully functional without WebSocket
# WebSocket only needed for real-time notifications
